// =================================================================================================
// WonderlandEngine OrbitalV2 Solar System Simulation - JS API Documentation Header
// -------------------------------------------------------------------------------------------------
// This file provides a concise, line-by-line documentation reference for the main classes, functions,
// and parameters available in the OrbitalV2 simulation codebase. Use this as a quick lookup for
// function signatures, class responsibilities, and editor-exposed parameters.
// -------------------------------------------------------------------------------------------------
// SYSTEM STRUCTURE:
//   - Physics: KeplerianOrbit.js (orbital mechanics)
//   - Data: body.js, rocketData.js (celestial/rocket data)
//   - Display: Conductor.js (main coordinator), CoordinateSystem.js (camera/coords)
//   - Rendering: CanvasManager.js, GridRenderer.js, UIRenderer.js, ColorUtils.js
//   - Planetary Render: PlanetRenderer.js, PlanetSphereRenderer.js, PlanetLabelRenderer.js, AtmosphericRenderer.js
//   - SandboxInput: button.js, ButtonUP-BS.js, ButtonDOWN-BS.js (UI controls)
// -------------------------------------------------------------------------------------------------
// KEY CLASSES & FUNCTIONS (with parameters and comments):
// -------------------------------------------------------------------------------------------------

// --- Importing Data ---
// To use planetary or atmospheric data, import the Body class:
//   import { Body } from './SolarSim/Data/body.js';
// Example: Access Earth's atmosphere layers:
//   const earthAtmosphere = Body.planetData.earth.atmosphere.layers;
// Example: Get a planet's position:
//   const earthPos = Body.initialConditions.earth.position;

// To get a planet's full object with all properties:
//   const earth = new Body('earth');
//   console.log(earth.atmosphere); // Atmospheric data
//   console.log(earth.position);   // Current position

// To get atmospheric summary for a planet (pressure, density, scale height):
//   const summary = earth.getAtmosphericSummary();
//   console.log(summary.surfaceDensity, summary.estimatedSurfacePressure);

// To get current orbital position (using Keplerian mechanics):
//   import { KeplerianBody } from './SolarSim/Physics/KeplerianOrbit.js';
//   earthKepler = new KeplerianBody('Earth', mass, radius, color, orbit);
//   earthKepler.updatePosition(currentTime);
//   console.log(earthKepler.position); // {x, y}

// To import and use rocket data:
//   import { RocketData } from './SolarSim/Data/rocketData.js';
//   const falconHeavy = RocketData.getRocketData('falconHeavy');
//   console.log(falconHeavy.thrust, falconHeavy.stages);

// To import and use rendering or simulation classes:
//   import { AtmosphericRenderer } from './SolarSim/Display/Rendering/Planetary Render/AtmosphericRenderer.js';
//   import { PlanetRenderer } from './SolarSim/Display/Rendering/Planetary Render/PlanetRenderer.js';
//   import { SimulationController } from './SolarSim/Display/Rendering/SimulationController.js';

// For coordinate transformations (world <-> screen):
//   import { UniversalCoordinateSystem } from './SolarSim/Display/CoordinateSystem.js';
//   const coords = new UniversalCoordinateSystem(width, height);
//   const screen = coords.worldToScreen(worldX, worldY);
//   const world = coords.screenToWorld(screenX, screenY);

// For color utilities:
//   import { ColorUtils } from './SolarSim/Display/Rendering/ColorUtils.js';
//   const rgba = ColorUtils.colorToRgba('#ffcc00', 0.5);

// For UI and grid rendering:
//   import { UIRenderer } from './SolarSim/Display/Rendering/UIRenderer.js';
//   import { GridRenderer } from './SolarSim/Display/Rendering/GridRenderer.js';

// For button and input components:
//   import { hapticFeedback } from './SolarSim/SandboxInput/button.js';
//   import { ButtonUP } from './SolarSim/SandboxInput/ButtonUP-BS.js';
//   import { ButtonDOWN } from './SolarSim/SandboxInput/ButtonDOWN-BS.js';

// All imports use ES6 module syntax. Adjust relative paths as needed for your file structure.

// --- Physics/KeplerianOrbit.js ---
// export class KeplerianOrbit
//   constructor(
//     semiMajorAxis,              // (meters) Size of orbit's long axis
//     eccentricity,               // (0-1) How stretched the orbit is (0=circle)
//     meanAnomalyAtEpoch,         // (radians) Position in orbit at epoch time
//     argumentOfPeriapsis=0,      // (radians) Angle to closest approach
//     longitudeOfAscendingNode=0, // (radians) Angle to orbit's ascending node
//     inclination=0,              // (radians) Tilt of orbit
//     epochTime=0                 // (seconds) Reference time for orbit
//   )
//   solveKeplersEquation(meanAnomaly, eccentricity, tolerance=1e-12)
//     // meanAnomaly: (radians) Current position in orbit
//     // eccentricity: (0-1) Orbit shape
//     // tolerance: (float) Solution accuracy
//   getStateAtTime(currentTime)
//     // currentTime: (seconds) Simulation time
//     // Returns {position, velocity, trueAnomaly, distance}
//   getOrbitalPeriod()
//     // Returns period in seconds
//   applyPerturbation(
//     deltaSemiMajorAxis,         // (meters) Change in orbit size
//     deltaEccentricity,          // Change in orbit shape
//     deltaMeanAnomaly,           // Change in position
//     deltaArgumentOfPeriapsis=0  // Change in closest approach angle
//   )

// export class KeplerianBody
//   constructor(
//     name,                       // (string) Body name
//     mass,                       // (kg) Mass
//     radius,                     // (meters) Physical radius
//     color,                      // (string/#hex) Display color
//     orbit                       // (KeplerianOrbit) Orbital parameters
//   )
//   getRadiusInMeters()           // Returns physical radius
//   getWorldRadius()              // Returns scaled radius for world
//   getDisplayRadius()            // Returns scaled radius for display
//   updatePosition(currentTime)   // Updates position for given time
//   addToTrail(maxTrailLength=2000) // Adds position to trail (max points)
//   getOrbitalPeriod()            // Returns orbital period
//   getDistanceFromFocus()        // Returns distance from central body
//   getTrueAnomaly()              // Returns current true anomaly
//   getOrbitalSpeed()             // Returns current orbital speed
//   getDistanceFrom(otherBody)    // Returns distance to another body

// export class PlanetarySystem
//   static createSolarSystem()    // Returns array of KeplerianBody (all planets)
//   static createInnerSolarSystem() // Returns array of KeplerianBody (inner planets only)

// --- Data/body.js ---
// export class Body
//   static planetData             // Dictionary of planet properties
//   static initialConditions      // NASA JPL initial positions
//   constructor(planetKey)        // planetKey: (string) Name of planet
//   initializeFromPlanet(planetKey) // Loads planet data
//   static calculateCircularOrbitVelocity(position, centralMass)
//     // position: (meters) Distance from center
//     // centralMass: (kg) Mass of central body
//   static getAtmosphereDensity(planet, altitude)
//     // planet: (Body) Planet object
//     // altitude: (meters) Height above surface
//   getMass()                     // Returns mass
//   getPosition()                 // Returns position {x, y}
//   getVelocity()                 // Returns velocity {vx, vy}
//   setPosition(x, y)             // Sets position
//   setVelocity(vx, vy)           // Sets velocity
//   setMass(mass)                 // Sets mass
//   setName(name)                 // Sets name
//   setColor(color)               // Sets color
//   setRadius(radius)             // Sets radius
//   getDisplayRadius()            // Returns display radius
//   getAtmosphereDensity(altitude)// Returns density at altitude
//   getAtmosphericScaleHeight()   // Returns scale height
//   getApproximateSurfacePressure() // Returns surface pressure
//   getSurfaceGravity()           // Returns surface gravity
//   getRadiusInMeters()           // Returns radius in meters
//   getWorldRadius()              // Returns scaled world radius
//   getMinDisplayRadius()         // Returns minimum display radius
//   getAtmosphericSummary()
//     // Returns {hasAtmosphere, surfaceDensity, estimatedSurfacePressure, scaleHeight, layerCount, maxAltitude, composition, color}

// --- Data/rocketData.js ---
// import { RocketData } from './SolarSim/Data/rocketData.js';
// To get a rocket's data:
//   const falconHeavy = RocketData.getRocketData('falconHeavy');
//   console.log(falconHeavy.thrust, falconHeavy.stages);

// --- Display/Conductor.js ---
// export class Drawer extends Component
//   static Properties
//     // material: (Material) Render material
//     // bgColor: (string/#hex) Background color
//     // paused: (bool) Pause simulation
//     // timeMultiplier: (float) Simulation speed
//     // showOrbits: (bool) Show orbital trails
//     // maxTrailLength: (int) Max trail points
//     // enablePerturbations: (bool) Enable orbit changes
//     // showOuterPlanets: (bool) Show outer planets
//     // useRealScale: (bool) Use physical scale
//     // planetScaleBoost: (float) Planet size multiplier
//     // cameraMode: (int) 1=Solar, 2=Inner, 3=Planet
//     // targetPlanet: (string) Planet to focus
//     // enableCameraSmoothing: (bool) Smooth camera
//     // manualZoom: (float) Zoom factor
//     // overridePlanetScaling: (bool) Manual scaling
//     // manualSunMultiplier: (float) Sun size multiplier
//     // manualPlanetMultiplier: (float) Planet size multiplier
//     // minPlanetPixels: (float) Minimum planet size
//   start()                       // Initializes simulation
//   update(dt)                    // dt: (seconds) Frame time
//   _render()                     // Renders frame
//   _startSimulationLoop()        // Starts simulation loop
//   _getCurrentModeString()       // Returns camera mode name
//   onDestroy()                   // Cleanup
//   setCameraMode(mode)           // mode: (int) Camera mode
//   setTargetPlanet(planetName)   // planetName: (string) Planet to focus

// --- Display/CoordinateSystem.js ---
// export class UniversalCoordinateSystem
//   constructor(canvasWidth=1024, canvasHeight=1024)
//     // canvasWidth: (pixels) Canvas width
//     // canvasHeight: (pixels) Canvas height
//   worldToScreen(worldX, worldY)
//     // worldX, worldY: (meters) World coordinates
//     // Returns {x, y} in pixels
//   screenToWorld(screenX, screenY)
//     // screenX, screenY: (pixels) Screen coordinates
//     // Returns {x, y} in meters
//   getPlanetSizeMultipliers()    // Returns {sunMultiplier, planetMultiplier}
//   getScaledRadius(body, useRealScale=true)
//     // body: (Body) Celestial body
//     // useRealScale: (bool) Use physical scale
//   setCameraModeByNumber(modeNumber, targetPlanet=null)
//     // modeNumber: (int) Camera mode
//     // targetPlanet: (Body) Planet to focus
//   setTarget(planet)             // planet: (Body) Planet to focus
//   getCameraMode()               // Returns current mode
//   setCameraMode(mode, targetPlanet=null)
//   updateCamera()                // Updates camera position
//   setCameraPosition(x, y)       // x, y: (meters) Camera position
//   setScale(scale)               // scale: (float) Meters per pixel
//   zoom(factor)                  // factor: (float) Zoom multiplier
//   getModeInfo()                 // Returns mode info object
//   getScaleDescription()         // Returns human-readable scale
//   getVisiblePlanets(allBodies)  // allBodies: (array) All bodies
//   isVisible(worldX, worldY, margin=100)
//     // worldX, worldY: (meters) Position
//     // margin: (pixels) Extra margin

// --- Display/Rendering/CanvasManager.js ---
// export class CanvasManager
//   constructor(engine, material, bgColor='#0a0a0a')
//     // engine: (WonderlandEngine) Engine instance
//     // material: (Material) Render material
//     // bgColor: (string/#hex) Background color
//   initialize()                  // Sets up canvas and texture
//   getContext()                  // Returns 2D context
//   updateTexture()               // Updates engine texture
//   clearCanvas()                 // Clears canvas

// --- Display/Rendering/GridRenderer.js ---
// export class GridRenderer
//   constructor(canvasManager, coordSystem)
//     // canvasManager: (CanvasManager) Canvas manager
//     // coordSystem: (UniversalCoordinateSystem) Coordinate system
//   drawReferenceGrid()           // Draws grid
//   drawTrail(body, showOrbits)   // body: (Body), showOrbits: (bool)
//   formatDistance(distance)      // distance: (meters)
//   _colorToRgba(color, alpha)    // color: (#hex/string), alpha: (float)

// --- Display/Rendering/UIRenderer.js ---
// export class UIRenderer
//   constructor(canvasManager, coordSystem, gridRenderer)
//   drawUI(
//     cameraMode,                 // (int) Camera mode
//     useRealScale,               // (bool) Use physical scale
//     planetScaleBoost,           // (float) Planet size multiplier
//     timeMultiplier,             // (float) Simulation speed
//     simulationTime,             // (seconds) Current time
//     targetPlanet,               // (string) Planet to focus
//     overridePlanetScaling,      // (bool) Manual scaling
//     bodies                      // (array) Celestial bodies
//   )
//   _drawPlanetModeInfo(ctx, canvas, targetPlanet, planetScaleBoost, bodies, cameraMode)
//   _drawInnerPlanetModeInfo(ctx, canvas)
//   _drawControlHints(ctx, canvas)
//   _formatPressure(pressure)     // pressure: (Pa)
//   _formatDensity(density)       // density: (kg/m^3)
//   _drawCameraModeIndicator(ctx, cameraMode, yOffset)

// --- Display/Rendering/ColorUtils.js ---
// export class ColorUtils
//   static parseAtmosphericColor(body) // body: (Body)
//   static adjustColorBrightness(color, factor) // color: (#hex/string), factor: (float)
//   static colorToRgba(color, alpha)  // color: (#hex/string), alpha: (float)

// --- Display/Rendering/CameraController.js ---
// export class CameraController
//   constructor(canvasWidth, canvasHeight)
//   initCamera(
//     cameraMode,                 // (int) Camera mode
//     targetPlanet,               // (string) Planet to focus
//     bodies,                     // (array) Celestial bodies
//     enableCameraSmoothing,      // (bool) Smooth camera
//     planetScaleBoost,           // (float) Planet size multiplier
//     overridePlanetScaling,      // (bool) Manual scaling
//     manualSunMultiplier,        // (float) Sun size multiplier
//     manualPlanetMultiplier,     // (float) Planet size multiplier
//     minPlanetPixels,            // (float) Minimum planet size
//     manualZoom                  // (float) Zoom factor
//   )
//   updateCamera(cameraMode, targetPlanet, bodies, planetScaleBoost, overridePlanetScaling, manualSunMultiplier, manualPlanetMultiplier, minPlanetPixels)

// --- Display/Rendering/Planetary Render/PlanetRenderer.js ---
// export class PlanetRenderer
//   constructor(canvasManager, coordSystem)
//   drawBodyWithAtmosphere(
//     body,                       // (Body) Celestial body
//     useRealScale,               // (bool) Use physical scale
//     minPlanetPixels,            // (float) Minimum planet size
//     cameraMode,                 // (int) Camera mode
//     planetScaleBoost           // (float) Planet size multiplier
//   )

// --- Display/Rendering/Planetary Render/PlanetSphereRenderer.js ---
// export class PlanetSphereRenderer
//   constructor(canvasManager, coordSystem)
//   drawPlanetSphere(
//     body,                       // (Body) Celestial body
//     screenPos,                  // ({x, y}) Screen position
//     planetRadius,               // (pixels) Display radius
//     cameraMode                  // (int) Camera mode
//   )
//   calculateDisplayRadius(
//     body,                       // (Body) Celestial body
//     useRealScale,               // (bool) Use physical scale
//     minPlanetPixels,            // (float) Minimum planet size
//     cameraMode,                 // (int) Camera mode
//     planetScaleBoost            // (float) Planet size multiplier
//   )

// --- Display/Rendering/Planetary Render/PlanetLabelRenderer.js ---
// export class PlanetLabelRenderer
//   constructor(canvasManager)
//   drawPlanetLabels(
//     body,                       // (Body) Celestial body
//     screenPos,                  // ({x, y}) Screen position
//     planetRadius,               // (pixels) Planet radius
//     atmosphericRadius,          // (pixels) Atmosphere radius
//     useRealScale,               // (bool) Use physical scale
//     cameraMode                  // (int) Camera mode
//   )
//   shouldDrawLabels(
//     planetRadius,               // (pixels) Planet radius
//     cameraMode                  // (int) Camera mode
//   )

// --- SandboxInput/button.js ---
// export function hapticFeedback(
//   object,                       // (Object) Has input component
//   strength,                     // (float) 0.0-1.0
//   duration                      // (ms) Pulse duration
// )
// export class ButtonComponent extends Component
//   static Properties: buttonMeshObject, hoverMaterial
//   start(), onActivate(), onDeactivate()
//   onHover, onDown, onUp, onUnhover

// --- SandboxInput/ButtonUP-BS.js ---
// export class ButtonUP extends Component
//   static Properties: drawerObject, targetCameraMode
//   start(), onDown, onUp

// --- SandboxInput/ButtonDOWN-BS.js ---
// export class ButtonDOWN extends Component
//   static Properties: drawerObject
//   start(), onDown, onUp

// =================================================================================================
// QUICK PARAMETER REFERENCE (for editor and code):
//   - cameraMode: (int) 1=Solar System, 2=Inner Planets, 3=Planet, 4=Atmosphere
//   - targetPlanet: (string) 'Earth', 'Mars', etc.
//   - planetScaleBoost: (float) Multiplies planet size in mode 3/4
//   - showAtmospheres: (bool) Enables atmospheric rendering
//   - atmosphereOpacity: (float, 0-1) Transparency of atmosphere
//   - useRealScale: (bool) True for physical scale, false for display scale
//   - overridePlanetScaling: (bool) Enables manual scaling
//   - manualSunMultiplier, manualPlanetMultiplier: (float) Manual scaling
//   - minPlanetPixels: (float) Minimum planet size in pixels
//   - paused: (bool) Pauses simulation
//   - timeMultiplier: (float) Simulation speed
//   - showOrbits: (bool) Shows orbital trails
//   - showOuterPlanets: (bool) Toggles outer planet visibility
//   - enableCameraSmoothing: (bool) Enables smooth camera transitions
//   - manualZoom: (float) Zoom factor
// =================================================================================================
// Use this header as a cheat sheet for function calls, class responsibilities, and parameter names.
// For details, see the corresponding file for implementation.
// =================================================================================================